<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Connexion – SUPCOUNT</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap & Fonts -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

    <!-- Style -->
    <link rel="stylesheet" href="{{ asset('css/login.css') }}">
</head>
<body>

    <!-- Logo -->
    <div class="text-center mt-4">
        <img src="{{ asset('images/supcount.png') }}" alt="SUPCOUNT Logo" class="logo">
    </div>

    <!-- Formulaire -->
    <div class="form-container">
        <h2 class="form-title text-center mb-4">Connexion</h2>

        <form id="login-form">
            <div class="mb-3">
                <label for="email" class="form-label">Adresse Email</label>
                <input type="email" name="email" id="email" class="form-control" placeholder="Ex: moi@email.com" required>
            </div>

            <div class="mb-4">
                <label for="password" class="form-label">Mot de passe</label>
                <input type="password" name="password" id="password" class="form-control" placeholder="••••••••" required>
            </div>

            <button type="submit" class="btn btn-primary w-100">Se connecter</button>
        </form>

        <p id="login-result"></p>

        <div class="form-text-link">
            <span>Pas encore de compte ? <a href="/register">S'inscrire</a></span>
        </div>
    </div>

    <script src="{{ asset('js/login.js') }}"></script>

</body>
</html>
